---
import Collapsible from './Collapsible.astro';
import { education } from '../data/content';
---

<section id="education" class="mx-auto max-w-5xl px-6 py-2">
  <Collapsible number="04" title="Education">
    <div class="space-y-4">
      {education.map((item) => (
        <div class="card reveal grid gap-3 p-6 sm:grid-cols-4 sm:gap-8">
          <p class="text-sm font-semibold text-teal-300 sm:col-span-1">{item.dates}</p>
          <div class="sm:col-span-3">
            <h3 class="text-lg font-bold text-white">{item.school}</h3>
            <p class="text-sm font-medium text-slate-400">{item.degree}</p>
            {item.description && (
              <p class="mt-2 text-sm leading-relaxed text-slate-400">{item.description}</p>
            )}
          </div>
        </div>
      ))}
    </div>
  </Collapsible>
</section>
